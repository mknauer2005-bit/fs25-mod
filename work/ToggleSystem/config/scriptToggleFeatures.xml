<?xml version="1.0" encoding="utf-8" standalone="no" ?>
<toggleFeatures>
 
	<feature
        id="farmlandProgressGate.enabled"
        description="Очки репутации и прогрессия участков"
        default="true"
        targetObject="FarmlandProgressGate"
        targetPath="enabled" />

    <feature
        id="randomTheftEvents.enabled"
        description="Случайные события (мод целиком)"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="enabled" />

    <feature
        id="randomTheftEvents.startupMissingVehicle.enabled"
        parentId="randomTheftEvents.enabled"
        description="Кража техники при входе в игру"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="startupMissingVehicle.enabled" />

    <feature
        id="randomTheftEvents.events.fuel.enabled"
        parentId="randomTheftEvents.enabled"
        description="Кража топлива"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="events.fuel.enabled" />

    <feature
        id="randomTheftEvents.events.seeds.enabled"
        parentId="randomTheftEvents.enabled"
        description="Кража семян"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="events.seeds.enabled" />

    <feature
        id="randomTheftEvents.events.fertilizer.enabled"
        parentId="randomTheftEvents.enabled"
        description="Кража удобрений"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="events.fertilizer.enabled" />

    <feature
        id="randomTheftEvents.globalEvents.drought.enabled"
        parentId="randomTheftEvents.enabled"
        description="Глобальное событие: засуха"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="globalEvents.drought.enabled" />

    <feature
        id="randomTheftEvents.globalEvents.flood.enabled"
        parentId="randomTheftEvents.enabled"
        description="Глобальное событие: наводнение"
        default="true"
        targetObject="g_randomTheftEvents"
        targetPath="globalEvents.flood.enabled" />
</toggleFeatures>
